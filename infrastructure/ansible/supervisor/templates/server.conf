[program:server]
command=python3 manage.py runserver

# gunicorn linkalytics:app -c /home/ansible/linkalytics/gunicorn.conf.py
process_name=%(program_name)s

directory=/home/ansible

stopsignal=TERM

autostart=true
autorestart=true

environment=PRODUCTION="Yes"

