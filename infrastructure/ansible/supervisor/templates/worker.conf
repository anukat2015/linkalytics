[program:queue_worker]
command=/usr/bin/python3 -m src {{ queues }}
process_name=%(program_name)s

directory=/home/ansible

stopsignal=TERM

autostart=true
autorestart=true

environment=PRODUCTION="Yes"
